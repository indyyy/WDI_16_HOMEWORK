var count
var multiplier
for (multiplier=1; multiplier<=10; multiplier++) { 
    for (count=1; count<=10; count++) {
        console.log(count + " x " + multiplier  + " = " + count*multiplier)
    }
}